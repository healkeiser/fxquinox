# Folder
$STEP$:
  type: folder
  metadata:
    creator: "fxquinox"
    entity: "step"
    name: "$STEP$"
    path: "$PATH$"
    parent: "$PARENT$"
    description: "Step folder for the shot, containing the tasks"
