# Folder
$TASK$:
  type: folder
  metadata:
    creator: "fxquinox"
    entity: "task"
    name: "$TASK$"
    path: "$PATH$"
    parent: "$PARENT$"
    description: "Task folder for the shot, containing the workfiles"
